spring.application.name=logprocessor

# ==== MySQL Database Configuration ====
spring.datasource.url=jdbc:mysql://google/logprocessor_db?cloudSqlInstance=log-processor-479817:us-central1:logprocessor-db
spring.datasource.username=springuser
spring.datasource.password=TheJavaBeans4156
server.port=${PORT:8080}


# MySQL driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Optional: cleaner logs
spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=warn
spring.main.banner-mode=off

# Enable Tomcat access logs (unchanged)
server.tomcat.accesslog.enabled=true
server.tomcat.accesslog.directory=logs
server.tomcat.accesslog.prefix=access_log
server.tomcat.accesslog.suffix=.log
server.tomcat.accesslog.file-date-format=.yyyy-MM-dd
server.tomcat.accesslog.pattern=%h %l %u %t "%r" %s %b
server.tomcat.basedir=.

spring.jpa.hibernate.ddl-auto=none
spring.datasource.initialization-mode=never

